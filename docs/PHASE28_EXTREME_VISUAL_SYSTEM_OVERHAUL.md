# Phase 28 — Extreme Visual + System Overhaul

Date: 2026-08-15
Status: SOURCE-INTEGRATED / TESTING — not VERIFIED

## Objective
Replace the Phase 27 visual layer with a coherent premium futuristic/cinematic/luxury identity that is implemented in real runtime paths, driven by real event evidence, accessible/performance-bounded, and presented in Thai throughout user-visible product surfaces.

## Implemented runtime architecture
- Dashboard realtime stage: Canvas particles, waves, orbit motion, event bursts, pointer parallax, CSS perspective/3D crystal/hologram composition and animated emoji feedback.
- Performance governor: bounded particles and DPR, ResizeObserver, hidden-tab suppression, `prefers-reduced-motion`, device tier and sustained-FPS degradation.
- Discord: supported Components V2/media/managed-message edits only. No false claim of native Canvas/WebGL/3D support.
- Realtime bridge: actual event publishers feed WebSocket/runtime visual directives; durable living-panel projection remains de-duplicated and coalesced.

## Audited event evidence
Real source paths include member join, ticket create/claim/close/reopen, community-event create/register/cancel/check-in, Gaming XP award/level-up, security alerts and existing setup/job/maintenance/backup/recovery/integration/scheduler state. Visual consumers never create the domain success event they visualize.

## Thai presentation
- 87 managed panel titles/descriptions and 119 action labels are Thai at catalog source.
- Blueprint-managed role/category/channel/forum-tag names and audit reasons are Thai desired-state presentation.
- Dashboard/Discord statuses and system-owned helper/modal copy use Thai presentation maps while exact technical keys/enums remain stable when operationally required.
- Asset metadata/badges/pulse labels use Thai source copy.

## Error boundary
Discord and HTTP presentation no longer echoes arbitrary backend `error.message` values. Raw exceptions remain server/operator diagnostics; users receive bounded Thai guidance plus a safe code/reference when one is available.

## Asset governance
333 current visual files are governed: 103 panel media + 230 theme media. Offline regeneration uses the prismatic-depth-v2 grammar and rebuilds manifests from final bytes/SHA/dimensions/frame counts after successful output.

## Current evidence
- Phase 28 extreme-overhaul: PASS 5,762 assertions.
- Thai presentation audit: PASS 5,818 assertions.
- TypeScript parser: PASS 180 source files.
- UI V2: PASS 8 blueprints / 415 Omni resources / 87 panels / 103 panel media references.
- A11y/i18n: PASS 14 static assertions.
- Offline release preflight: PASS 31 gates.

## Verification boundary
These are source/offline contracts. A reviewed `package-lock.json` is still absent, so dependency-backed semantic typecheck/Vitest/build/audit/SBOM is blocked. Migration 054, live Discord message editing/rate limits, deployed WebSocket behavior and representative desktop/mobile GPU/FPS/accessibility evidence remain pending approved targets. No production verification claim is made.
